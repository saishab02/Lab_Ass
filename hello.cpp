#include <iostream>
int main() {
    // std::cout is used to output data to the console (standard output)
    std::cout << "Hello, World! This is a simple test." << std::endl;

    // A return value of 0 typically indicates successful execution
    return 0;
}